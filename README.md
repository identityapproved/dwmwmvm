# vmdots

## Prerequisites

- Arch Linux with `base-devel` and `git` installed.
- An AUR helper (`yay` or `paru`) already available on the path.
- Ability to run commands with `sudo` for package installs and configuration.
