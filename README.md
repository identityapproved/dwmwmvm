# vmdots

## Prerequisites

- Arch Linux with `sudo` configured.
- Network access to clone AUR helpers and repositories during setup.

The installer will automatically install `base-devel`, `git`, and your chosen AUR helper (`yay` by default) if they are not already present.
